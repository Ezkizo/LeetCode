//obtener el path de la raiz del proyecto el txt de la dirección
using System;
using System.IO;

var archivo = Path.GetFullPath("input1.txt");

var txt = File.ReadAllText(archivo);

Console.WriteLine(txt);
int[][] combo = new int[txt.Length][];



static int checkList()
{
    return 0;
}